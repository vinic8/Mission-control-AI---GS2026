# Template de variáveis de ambiente da Mission Control AI.
# Copie este arquivo para .env e coloque a sua chave real (sem aspas).
# IMPORTANTE: o arquivo .env está no .gitignore e NUNCA deve ser commitado.

OLLAMA_API_KEY="sua_chave_aqui_sem_aspas"
